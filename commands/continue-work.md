---
description: Continue an existing initiative. Loads repo state, picks next slice, implements with TDD, verifies, updates docs.
---

**Initiative:** $ARGUMENTS

Use the `execution-loop` skill. Follow it exactly — all steps, all gates, all output formats.

The skill handles: session start checklist → stale check → load state → select slice → scope → research → TDD → ECC delegation → verify → debug if needed → re-assess → update docs → learning → stop.

Interpret the argument as the initiative name, optionally with a specific constraint or priority.
