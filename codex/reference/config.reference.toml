# Codex reference config inspired by everything-claude-code.
#
# This file is intentionally NOT installed as ~/.codex/config.toml.
# Merge only the sections that fit your existing setup.

approval_policy = "on-request"
sandbox_mode = "workspace-write"

[features]
multi_agent = true

[agents]
max_threads = 6
max_depth = 1

# These config_file paths assume you merged these sections into ~/.codex/config.toml
# after installing the reference bundle.
[agents.explorer]
description = "Read-only codebase explorer for gathering evidence before changes are proposed."
config_file = "bootstrap-reference/claude-code-bootstrap-commands/agents/explorer.toml"

[agents.reviewer]
description = "Reviewer focused on correctness, security, regressions, and missing tests."
config_file = "bootstrap-reference/claude-code-bootstrap-commands/agents/reviewer.toml"

[agents.docs_researcher]
description = "Documentation specialist that verifies APIs and framework behavior against primary docs."
config_file = "bootstrap-reference/claude-code-bootstrap-commands/agents/docs-researcher.toml"

# Optional MCP examples. Add only when you already use them and have credentials.
# [mcp_servers.github]
# url = "https://api.githubcopilot.com/mcp/"
#
# [mcp_servers.openaiDeveloperDocs]
# url = "https://developers.openai.com/mcp"
